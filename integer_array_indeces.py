import numpy as np

arr = np.array([10, 20, 30, 40, 50])
indices = np.array([1, 3, 4])
print(arr[indices])    # Output: [20 40 50]

# the given indices array will act as a collection of indices of the original array